<%= form_for @bdtipo, :html => { :class => "form-horizontal bdtipo" } do |f| %>

  <% if @bdtipo.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@bdtipo.errors.count, "error") %> prohibited this bdtipo from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @bdtipo.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :tipo_sensor, :class => 'control-label col-lg-2' %>
    <div class="col-lg-10">
      <%= f.text_field :tipo_sensor, :class => 'form-control' %>
    </div>
    <%=f.error_span(:tipo_sensor) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit nil, :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                bdtipos_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
